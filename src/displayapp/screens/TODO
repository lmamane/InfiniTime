 * IFR hold timers; icon 0xf021  (or 0xf2f1 if it is already there to save size)

We could save a few bytes by storing:
 * previousIFRTime in seconds as int32 or even int16 (already > 18h)
 * BlocksOffTime as it is
 * all other times as unsigned (positive) int16 offsets to BlocksOffTime
 * do not store currentFlightState per se, but implicit by zero-ness
   of corresponding time.
